#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int countPartitions(vector<int>& arr, int d) {
        int n = arr.size();
    }
};

int main() {
    Solution sol;
    vector<int> arr = {1, 2, 3, 4};
    int d = 1;
    cout << sol.countPartitions(arr, d) << endl;
    return 0;
}
